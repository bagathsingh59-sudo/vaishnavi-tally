# Streamlit Community Cloud secrets — copy this to the Streamlit Cloud dashboard
# Settings → Secrets (do NOT commit the real secrets.toml to git)

ENV = "production"
DB_NAME = "vaishnavi_tally"
FIRM_NAME = "Vaishnavi Consultants"

# Paste your MongoDB Atlas connection string below
MONGO_URI_PROD = "mongodb+srv://<user>:<password>@cluster.mongodb.net/"
